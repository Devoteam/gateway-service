# Server
server.port = 8080

# Eureka
spring.application.name = gateway-service

# Enable Spring Cloud Gateway to create routes based on Eureka discovery
spring.cloud.gateway.discovery.locator.enabled = true